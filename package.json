{
  "name": "eval3",
  "version": "11.0.5",
  "description": "",
  "main": "index.html",
  "author": {
    "name": "btopro"
  },
  "license": "Apache-2.0",
  "private": false,
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "start": "npm run serve",
    "ghpages:build": "npm run build:custom && rm -rf custom/node_modules && rm -f index.html && cp ghpages.html index.html && rm -f assets/babel* && rm -rf build && rm -f wc-registry.json",
    "build:custom": "cd custom && npm install && npm run build",
    "serve": "npx @haxtheweb/haxcms-nodejs",
    "dev": "NODE_ENV=development npm run serve"
  }
}